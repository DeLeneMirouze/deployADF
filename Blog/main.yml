pool:
  name: default

resources:
  repositories:
    - repository: myRepo1
      reaf: main
      type: github
      name: DeLeneMirouze/TimeHelper
      endpoint: Github
    - repository: myRepo2
      reaf: main
      type: github
      name: DeLeneMirouze/EscarGo
      endpoint: Github

# jobs:
# - job:
#   workspace:
#     clean: all
# steps:
#   - script: dir
# - checkout: myRepo2

steps:
  - script: $(Agent.WorkFolder)
  - script: $(Agent.BuildDirectory)
    
  - script: $(Build.SourcesDirectory)
  - script: $(System.DefaultWorkingDirectory)
    
  - script: $(Build.BinariesDirectory)
    
  - script: $(Build.StagingDirectory)
  - script: $(System.ArtifactsDirectory)
  - script: $(Build.ArtifactStagingDirectory)



