pool:
  name: default

resources:
  repositories:
    - repository: myRepo
      reaf: main
      type: github
      name: TimeHelper
      endpoint: Github

steps:
  - checkout: self
  - checkout: myRepos
