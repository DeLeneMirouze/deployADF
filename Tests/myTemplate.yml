# myTemplate.yml
parameters:
  - name: foo3
  - name: foo4

stages:
  - stage: Stage3
    dependsOn: [Stage1]
    jobs:
      - job:
        steps:
          # - script: echo ${{ parameters.foo4 }}
          - script: echo $(parameters.foo3 )
          # - script: echo $[ stageDependencies.Stage1.Job1.outputs['step1.bar'] ]
