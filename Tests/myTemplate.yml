# myTemplate.yml
# parameters:
#   - name: foo3
# # - name: foo4

stages:
  - stage: Stage3
    variables:
      foo4: $[ stageDependencies.Stage1.Job1.outputs['step1.bar'] ]
    dependsOn: [Stage1]
    jobs:
      - job:
        steps:
          - script: echo $(foo4)
